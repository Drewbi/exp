
name: Deploy

on:
  push:
    paths-ignore:
    - '.github/**'
    branches: 
    - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install Dependencies
        run: yarn
      
      - name: Build Distribution
        run: yarn build
